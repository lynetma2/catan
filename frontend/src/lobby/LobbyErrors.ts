export enum LobbyError {
    LOBBY_NOT_FOUND = 'Lobby not found. It may have been closed or never existed.',
    LOBBY_JOIN_REJECTED = 'Could not join the lobby. It may be full or already started.',
    LOBBY_CREATE_REJECTED = 'Could not create a new lobby. Please try again.',
}